
/**
 * Use meta.role to determine if the current user has permission
 * @param roles
 * @param route
 */

const state = {
}

const mutations = {
}

const actions = {
  generateRoutes ({ commit }, module) {
    return new Promise(resolve => {
      let accessedRoutes
      resolve(accessedRoutes)
    })
  }
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
